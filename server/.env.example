# Environment
NODE_ENV=production

# Server Configuration
PORT=5000
JWT_SECRET=your_jwt_secret_key_here

# Frontend URL (your Namecheap / Vercel / Netlify deployed URL)
CLIENT_URL=https://yourdomain.com

# Database Configuration
# On Render: set DATABASE_URL directly from the Render Postgres "Internal Database URL"
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Fallback individual DB vars (only used if DATABASE_URL is not set)
DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=your_db_name
DB_PASSWORD=your_db_password
DB_PORT=5432

# Email Configuration (Gmail App Password)
EMAIL_USER=your_gmail@gmail.com
EMAIL_PASS=your_app_password

# Stripe (if using payments)
STRIPE_SECRET_KEY=sk_live_...
